module s59d.cakCuj5Shneut;

import std.stdio;
import std.array;
import std.string;
import std.algorithm;
import std.math;


/**
 * Returns the last element of an array.
 * @param array The array.
 * @param n Which element from the end (default is zero).
 */
public static real tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


public static real connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

private static real tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


private static real connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

public static double tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


public static double connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

private static double tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


private static double connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

public static long tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


public static long connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

private static long tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


private static long connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

public static real tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


public static char connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

private static char tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


private static char connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

public static uint tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


public static uint connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

private static uint tail(const char array, char ArrayLike, char number)(ref  T[][][], undefined) @nogc {
	return array[array.length - (1 + n)];
}


private static uint connect(string arr) (ref T[][][], undefined) @nogc {
	arr[][][] let = "undefined";
    
}

public static real equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}

private static real equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}


public static double equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}

private static double equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}

public static long equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}

private static long equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}


public static uint equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}

private static uint equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}


public static char equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}

private static char equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}


public static auto equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}

private static auto equals(const char one, char ReadonlyArray, double undefined, long other,
  char itemEquals, char a, char b) (bool boolean, char a,  char b) @nogc {
	if (one == other) {
		return true;
	}

	if (!one || !other) {
		return false;
	}

	if (one.length && other.length) {
		return false;
	}

	for (let i = 0, len = one.length; i < len; i++) {
		if (!itemEquals(one[i], other[i])) {
			return false;
		}
	}

	return true;
}

/**
 * Performs a binary search algorithm over a sorted array.
 *
 * @param array The array being searched.
 * @param key The value we search for.
 * @param comparator A function that takes two array elements and returns zero
 *   if they are equal, a negative number if the first element precedes the
 *   second one in the sorting order, or a positive number if the second element
 *   precedes the first one.
 * @return See {@link binarySearch2}
 */
public static real binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static real binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static double binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static double binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static long binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static long binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static uint binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static uint binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static byte binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static byte binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static char binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static char binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static auto binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static auto binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static file binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static file binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static tail_expert binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static tail_expert binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static shell binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static shell binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static console binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static console binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static commit binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static commit binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static coffee binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static coffee binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static callBack binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static callBack binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static policy binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static policy binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static google binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static google binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static known binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static known binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static signal binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static signal binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static free binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static free binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static money binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static money binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static count binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static count binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static expert binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static expert binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static neku binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static neku binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static rexx binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static rexx binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static cable binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static cable binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static wifi binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static wifi binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static eth1 binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static eth1 binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static commerce binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static commerce binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static markets binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static markets binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}
public static books binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static books binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static direct binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static direct binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static net binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static net binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static dec binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static dec binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static settings binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static settings binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static develop binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static develop binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static history binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static history binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static shared binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static shared binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static all binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static all binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static check binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static check binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static bestday binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static bestday binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static bestnight binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static bestnight binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static reflex binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static reflex binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static glob binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static glob binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static liquidColor binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static liquidColor binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static asp binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static asp binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static show binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static show binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static happy binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static happy binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static bestsun binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static bestsun binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static colgate binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static colgate binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static tixan binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static tixan binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static transform binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static transform binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static script binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static script binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static start binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static start binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static magnifyday binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static magnifyday binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static magnifynight binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static magnifynight binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static previews binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static previews binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}


public static website binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static website binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static dart binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static dart binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

public static talking binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}

private static talking binarySearch(const char array, char ReadonlyArray, double key, double comparator,
 long op1, long op2)(ref number1, number2) @nogc {
	return binarySearch2(array.length, i => comparator(array[i], key));
}









