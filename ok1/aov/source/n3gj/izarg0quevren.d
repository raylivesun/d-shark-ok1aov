module n3gj.izarg0quevren;

import std.stdio;
import std.array;
import std.string;
import std.algorithm;
import std.math;

export class P1
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P2
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}


export class P3
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P4
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P5
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P6
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P7
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}


export class P8
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P9
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P10
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P11
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P12
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P13
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P14
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P15
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P16
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P17
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P18
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P19
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P20
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P21
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P22
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P23
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P24
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P25
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P26
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P27
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P28
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P29
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P30
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P31
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P32
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P33
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P34
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}

export class P35
{
public shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}

private shared pub_netcake commonPrefixLength(const char one, 
double ReadonlyArray, double other, 
double equals,
long a, long b) (ref boolean, a, b, number) {
	double let_result = 0;

	for (let i = 0, len = Math.min(one.length, other.length); i < len && equals(one[i], other[i]); i++) {
		result++;
	}

	return result;
}
}


