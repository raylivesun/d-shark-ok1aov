module n3gj.kirquiojmoov8;

import std.stdio;
import std.array;
import std.string;
import std.algorithm;
import std.math;

export class T1
{
public static child_pubcake range(const char to, double number) (ref number[]) {

    return number([to]);
    
}
private static child_pubcake range(const char to, double number) (ref number[]) {

    return number([to]);
    
}    
    
public static child_netcake range(const char from, double number1, 
long to, double number2) (ref number[]) {

   return number([from, to]);    
    
}        
public static child_netcake range(const char arg, double number1, 
long to, double number2) (ref result[]) {
    let network = arg;
	return result;
}
}


