module s59d.rhomghil;

import std.stdio;
import std.array;
import std.string;
import std.algorithm;
import std.math;

public static real forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static real forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static double forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static double forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static long forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static long forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static char forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static char forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static uint forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static uint forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static trumpbestday forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static trumpbestnight forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static tow_corn_six_mill forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static tow_corn_six_mill  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static third_corn_six_mill forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static third_corn_six_mill  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static four_corn_six_mill forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static four_corn_six_mill  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static bc_corn_six_mill forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static bc_corn_six_mill  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static gambas_richer_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static gambas_richer_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static list_corn_six_mill forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static list_corn_six_mill  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static explore_corn_six_mill forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static explore_corn_six_mill  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}



public static opera_corn_six_mill forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static opera_corn_six_mill  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static geek_corn_six_mill forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static geek_corn_six_mill  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static bib_richer_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static bib_richer_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static beybe_richer_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static beybe_richer_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static stream_corn_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static stream_corn_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static frunbueza_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static frunbueza_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static SpongeBob_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static SpongeBob_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static lunchbox_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static lunchbox_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static ground_meats_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static ground_meats_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static product_free_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static product_free_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_clothes_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_clothes_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_wallet_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_wallet_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_product_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_product_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_items_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_items_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_stream_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_stream_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_record_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_record_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_robert_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_roberts_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_southpark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_southpark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_zapping_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_zapping_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_richer_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_richer_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_robert_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_roberts_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_rotashark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_rotashark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_perlshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_perlshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_civilshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_civilshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_gsmshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_gsmshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_gsmshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_coolshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_coolshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_expshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_expshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_clownshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_clownshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_streamshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_streamshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_magnifyshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_magnifyshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_expertshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_expertshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_sundaymorning_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_sundaynights_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_dogman_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_dogman_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_weekshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_weekshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_victorshark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_vectorshark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_guava_juice_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_guava_juice_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_nature_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_nature_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumnight_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_best_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_best_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_trumpday_jobs_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnigth_jobs_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_works_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_works_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_document_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_document_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_trumpday_markets_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_markets_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_clothes_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_clothes_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_offices_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_offices_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_fashion_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_fashion_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_trumpday_shellscript_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_shellscript_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_trumpday_keys_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_keys_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_trumpday_sundaymorning_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_sundaynight_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_trumpday_hiphop_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_hiphop_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_trumpday_dscript_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_dscript_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_streampath_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_streampath_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_expert_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_expert_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


public static new_trumpday_directwifi_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_directwifi_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

public static new_trumpday_black_shark_six_millions forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++) {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}

private static new_trumpnight_black_shark_six_millions  forEachWithNeighbors(const char arr, double f, 
double before, double undefined, double element, double after) (ref T[][][]) @nogc {
	for (let i = 0; i < arr.length; i++)  {
		f(i == 0 ? undefined : arr[i - 1], arr[i], i + 1 == arr.length ? undefined : arr[i + 1]);
	}
}


